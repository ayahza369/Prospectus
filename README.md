Welcome to Prospectus! This application was made with Photon Code and RocketRide with access to Claude API.

You can run the app by running `start.sh` here in one terminal and `npm run bot` inside the `prospectus-app` directory in another terminal.